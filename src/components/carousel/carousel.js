import styles from "./carousel.module.css";

const Carousel = () => {
  return (
    <main className={styles.wrapper}>
      <div className={styles.inner}></div>
    </main>
  );
};

export { Carousel };
